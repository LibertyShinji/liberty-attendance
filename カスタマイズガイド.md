# カスタマイズガイド（自社に合わせた調整方法）

このシステムは中古車販売会社（正社員＋パート混在・13名・LINE通知フル活用）向けに作られています。
あなたの会社の事情に合わせて**削る・変える**ための地図です。改修を行うAI/開発者は、コードを触る前にここを読むこと。

## 0. まず決めること（この4つで改修範囲が決まる）

| 質問 | Yesなら | Noなら |
|---|---|---|
| パート（時給制）はいる？ | そのまま使える | §2（何もしなくても自動で隠れる） |
| 有給管理はアプリでやる？ | そのまま使える | §3で有給機能を外す |
| LINE通知（リマインド等）は欲しい？ | トリガー設定する | §4（トリガーを設定しなければ大半は止まる） |
| 休みの決め方は「基本出勤＋休みを登録」？ | そのまま使える | §5参照（シフト制には向かない） |

## 1. 雇用形態と公休上限

- 雇用形態は社員マスタI列（`正社員` / `パート`）。判定は文字列一致。
- **公休上限（正社員6日/パート15日）**は次の4か所にハードコードされている。変える場合は全部揃えること：
  - ShiftManagement.gs `getMyKibou` 内 `emp.empType === 'パート' ? 15 : 6`
  - ShiftManagement.gs `submitKibou` 内 同上
  - AdminDashboard.html `renderKoukyuuCalendar` 内 `types[empId] === 'パート' ? 15 : 6`
  - Code.gs 公休タブ関連のコメント表記
  - 上限を廃止したい（自由に申請させたい）場合は判定を大きな数にするのが最小改修。
- 全員正社員の会社：何も変えなくてよい（全員6日上限）。上限日数だけ自社ルールに合わせて変更。

## 2. パート給与機能（パートがいないなら放置でOK）

- 給与カード（本人）・💴給与タブ（管理者）は**雇用形態がパートの社員がいなければ実質何も表示されない**
  （本人カードは isPart:false で自動非表示）。削らなくても害はない。
- 完全に消したい場合：Calendar.html の payCard 一式＋`get_my_payroll`/`set_my_target` ルート＋
  Code.gs `computePartPayroll_`/`getMyPayroll`/`setMyTarget`、AdminDashboard.html 給与タブ。
- 使う場合の設定：⚙社員タブで時給3種（平日/土曜/日祝）・固定控除額（社保）・控除率（雇用保険等）を入力。
  祝日判定は Code.gs の `JP_HOLIDAYS_YYYY` 定数——**運用する年の分を毎年追加すること**。

## 3. 有給機能を使わない場合

- リッチメニューから「有給申請」ボタンを外す（`setupRichMenu6` のボタン定義と画像を変更）。
- 管理者画面の「📝有給」「📅残日数」タブを削除（AdminDashboard.html）。
- 有給を参照している読み取り側（カレンダー・月次・リマインド除外・公休タブの緑表示）は
  データが無ければ何も表示しないだけなので、**バックエンドは削らなくても動く**。UIだけ外すのが安全。

## 4. LINE通知の削り方（重要：段階がある）

通知は用途別に独立している。「どこまで要るか」で選ぶ：

| 通知 | 止め方 |
|---|---|
| 出勤忘れ・退勤忘れリマインド／退勤もれ役員通知 | **時間主導トリガー（setupClockOutReminderTrigger）を設定しなければ一切動かない**。導入時に様子見するなら ScriptProperty `COREMINDER_DRYRUN` / `CIREMINDER_DRYRUN` を 'true'（ログのみ・送信なし） |
| 有給申請・休み希望の管理者への承認依頼DM | 残したほうがよい（これが無いと申請に気づけない）。不要なら notifyAdminsOfLeaveRequest / submitKibou 内の送信部を削除 |
| 急な休みの全員通知 | notifySuddenAbsence の staffTargets を空にすれば役員のみに |
| 時間外打刻・休日打刻の管理者通知 | notifyAdminsOutOfHours / notifyAdminsHolidayPunch を空関数に |
| **全LINE通知を一括停止** | Code.gs の `sendMessage` 関数の先頭に `return;` を入れる。**画面（LIFF）は全部そのまま動く**＝「通知なし・画面だけ」運用が最小構成 |

- LINE公式アカウントのフリープランは月200通まで。リマインド系をフルに使うと人数×回数で意外と超える
  （元の会社は13名で月2,600通≒有料ライトプラン）。通数を抑えたいならリマインドの時間帯・間隔
  （COREMINDER/CIREMINDER定数のSTART_MIN/END_MIN/GAP_MS）を狭めるか、リマインド自体を使わない。
- そもそもLINEを使わない選択肢は**ない**（本人認証がLIFFのuserId前提のため）。通知だけが任意。

## 5. 勤務モデルの前提（シフト制の会社は注意）

- このシステムは**「全日出勤がデフォルト・休む日だけ登録する」公休モデル**。
  固定シフト制（出勤日を登録する方式）や時間帯シフトには向かない。その場合は逆転の発想で
  「休み＝シフトが入っていない日」を公休として一括登録する運用にするか、大きめの改修が要る。
- 公休を登録していない全社休業日（年末年始・臨時休業）は**欠勤としてカウントされる**。
  休業日も公休として全員分登録する運用が前提（管理者の公休タブで日付タップ）。
- 半休の時間帯表記（午前10:00-14:00/午後14:00-18:00）は leave.html と Calendar.html の**表示文言だけ**。
  ロジックは0.5日としか扱っていないので、自社の時間帯に文言を直すだけでよい。

## 6. その他の会社差分

- **年度**：有給は4月始まり（Code.gs `fiscalYear()`）。1月始まりにするなら fiscalYear と updateAnnualLeave を変更。
- **繰越**：前年度残の繰越上限20日（updateAnnualLeave内）。自社規程に合わせて変更。
- **役員（isAdmin）は打刻できない仕様**。社長も打刻したい会社は getTodayStatus / liffClockIn / liffClockOut の
  isAdminブロックを外す改修が必要（承認権限と打刻を分ける列を足すのが丁寧）。
- **勤務時間帯チェック**：9:00〜20:00外の打刻で管理者通知（isOutOfHours）。営業時間に合わせて変更。
- **リッチメニュー**：ボタン構成・画像は RichMenuImage.gs（base64）＋ setupRichMenu6。画像条件=幅800-2500・
  縦横比1.45以上・1MB未満。同梱の richmenu6_lineart.jpg を差し替え元にできる。
- **GPS記録**：打刻時に位置を記録（ブロックはしない）。不要なら Attendance.html の位置取得と
  打刻APIの loc 引数まわりを削除。

## 7. 削ってはいけないもの

- doPost ルーター構造・`fmt`（Asia/Tokyo整形）・`text/plain` fetch規約 — 全画面が依存。
- 有給承認の LockService と「有給変更履歴」ログ — 2重承認事故の再発防止装置。
- 公休設定シートの「確定/希望/取消希望」3ステータスの規約 — 全集計がこれを前提（詳細は開発ガイド）。
